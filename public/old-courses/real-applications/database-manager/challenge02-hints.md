# Hints

## 🟢 Hint 1
Loop through `db` to find records by ID.

## 🟡 Hint 2
To update Iron Man (ID 1):
`if user["id"] == 1: user["active"] = False`

## 🔴 Hint 3
To delete Ultron (ID 2):
Find him first, save to a variable, then `db.remove(variable)`.
