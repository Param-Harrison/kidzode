# Solution

```python
db = []

db.append({"id": 1, "name": "Zelda"})
db.append({"id": 2, "name": "Link"})
db.append({"id": 3, "name": "Ganon"})

for user in db:
    print(user["name"])
```

Output:
```
Zelda
Link
Ganon
```
