# Hints

## 🟢 Hint 1
Did you `import json`?

## 🟡 Hint 2
Use `json.loads(data_string)` to get a dictionary.

## 🔴 Hint 3
Save it to a variable: `user = json.loads(data_string)`
Then print: `print(f"Name: {user['name']}")`
