# Hints

## 🟢 Hint 1
First, parse it: `data = json.loads(data_string)`

## 🟡 Hint 2
Get the list: `user_list = data['users']`

## 🔴 Hint 3
Loop it:
```python
for player in user_list:
    print(f"Player: {player}")
```
