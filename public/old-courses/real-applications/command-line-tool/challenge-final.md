# Final Challenge: To-Do CLI ✅

Build a full To-Do List manager using `argparse`.

**Mockup:**
1. `todo.py add "Buy Milk" --priority high` -> Adds to list with [HIGH].
2. `todo.py list` -> Lists all items.
3. `todo.py clear` -> Clears list.

**Requirements:**
- Use `argparse`.
- Support commands: `add`, `list`, `clear`.
- `add` takes a required string `task` and optional `--priority`.
- Use Colors for High Priority! 🔴

*Hint: Use a global list or simulate a file for storage.*

Go organize your life!
