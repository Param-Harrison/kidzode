# Mini-Challenge 2: The Spy 🕵️

Create a decorator called `@spy`.
It should print "Spying on [function name]..." before running the function.

**Your Goal:**
1. Define `spy`.
2. Decorate `def say_secret(msg): print(msg)`.
3. Call `say_secret("The treasure is hidden")`.

Go!
