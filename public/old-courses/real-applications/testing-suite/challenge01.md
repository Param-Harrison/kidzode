# Mini-Challenge 1: Math Tester

Here is a function:
```python
def is_even(n):
    return n % 2 == 0
```

**Your Goal:**
1. Define a function `test_is_even()`.
2. Assert that `is_even(2)` is True.
3. Assert that `is_even(3)` is False.
4. Run your test function.

Go make sure math is real! 🧮
