# Hints

## 🟢 Hint 1
Loop using `enumerate(documents)` to get `doc_id` (0, 1...).

## 🟡 Hint 2
Clean: `doc.translate(...)` then `lower()`.

## 🔴 Hint 3
Index: `if word not in stop_words:` add to index.
