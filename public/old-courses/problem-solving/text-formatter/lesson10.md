# Join Text

Put the list back together!
Use `.join()`.

```python
words = ['I', 'love', 'coding']
sentence = " ".join(words)

print(sentence)
```

You'll see:
```
I love coding
```

We joined them with a space `" "`!

## Try This!
Join them with a dash `"-"` instead!
