# Hints

## 🟢 Hint 1
Define the function: `def is_strong(password):`

## 🟡 Hint 2
Check length: `if len(password) >= 8:`

## 🟠 Hint 3
Check symbol: `if "!" in password:`

## 🔴 Hint 4
Combine them with `and`: `if len(password) >= 8 and "!" in password:`
