# Basic Multiplication

Let's do some math! We know `+` and `-`. Now let's use `*` for times!

```python
print(3 * 4)
```

You'll see:
```
12
```

Computers are great at math!

## Try This!
Calculate `7 * 8` and `9 * 9`!
