# CHALLENGE 1: Transfer Funds 🔁

Move money from Alice to Bob.
This involves TWO objects.

### Your Task:
1. `Account` has `deposit` and `withdraw`.
2. Create `a1` ($100) and `a2` ($0).
3. `a1.withdraw(50)`.
4. `a2.deposit(50)`.
5. Print both balances.

### Achievement:
You processed a wire transfer! 🏦
