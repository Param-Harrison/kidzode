# CHALLENGE 3: Activity Log 📜

Banks keep a history of transactions.
Store strings in a list inside the object!

### Your Task:
1. `__init__`: `self.history = []`.
2. In `deposit`, `append("Deposited " + str(amt))`.
3. Do some deposits.
4. Print `self.history`.

### Achievement:
You built a auditing system! 🗂️
