# Hints for Game Loop

## 🟢 Hint 1
Define methods first:
```python
def feed(self):
    self.energy += 5
```

## 🟡 Hint 2
Check input string:
```python
if action == "feed":
    p.feed()
```

## 🟠 Hint 3
Use `while True:` to keep the game running forever (until stopped).
