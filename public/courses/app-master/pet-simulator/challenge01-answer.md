### One way to solve it:

```python
class Pet:
    def __init__(self, n, s):
        self.name = n
        self.species = s

my_pet = Pet("Perry", "Platypus")
print(my_pet.name, "is a", my_pet.species)
```
