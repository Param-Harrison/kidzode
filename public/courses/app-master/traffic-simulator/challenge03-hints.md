# Hints for Crash Detection

## 🟢 Hint 1
Position property:
`c.x`

## 🟡 Hint 2
Equality:
`==` checks if they match exactly.

## 🟠 Hint 3
Logic:
You don't need a method, just `if c1.x == c2.x:`.
