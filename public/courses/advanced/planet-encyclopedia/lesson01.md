# Better Data 🪐

A list is great for storing names, like `["Mars", "Venus"]`.
But what if we want to store the **size** and **color** too?

### The Solution: Dictionaries
A dictionary uses `{}` and lets us label our data.

```python
mars = {
  "name": "Mars",
  "color": "Red",
  "size": 6779
}
```

### Your Goal
1. Create a dictionary for a planet called `planet`.
2. Give it a `"name"`, `"type"`, and `"moons"`.
3. Print the planet's name using `planet["name"]`.

### Achievement
📖 **Data Architect**: You built a structured data record!
