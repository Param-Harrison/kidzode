### One way to solve it:

```python
raw_data = "I hate messy code. It is bad."

print("Original:", raw_data)

# Cleaning the data...
clean_data = raw_data.replace("hate", "love").replace("bad", "good")

print("Cleaned:", clean_data)
```
