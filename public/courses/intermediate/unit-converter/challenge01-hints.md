# Hints for Unit Converter Challenge

## 🟢 Hint 1
Define your functions at the top of the file!

## 🟡 Hint 2
Use `float(input())` if you want the user to be able to type decimals (like 10.5).

## 🟠 Hint 3
Your `if` statement should look like this:
```python
if choice == "1":
    print(km_to_miles(val))
```
