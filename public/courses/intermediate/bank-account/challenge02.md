# CHALLENGE 2: Transfer Money 💸

Sending money to a friend? Let's write a function for that!

### Your Task:
1. Create a variable `my_balance = 100`.
2. Create a variable `friend_balance = 0`.
3. Write a function `transfer(amount)`.
4. Subtract from `my_balance` and Add to `friend_balance`.
5. Don't forget to use `global` for BOTH variables!

### Achievement:
You've engineered a multi-account transaction system! 🏦
