# Hints for Race Timer Challenge

## 🟢 Hint 1
Getting the goal: `goal = int(input(...))`.

## 🟡 Hint 2
To count UP:
```python
for i in range(1, goal + 1):
    print(i)
    time.sleep(0.5) # Fast count!
```

## 🟠 Hint 3
The `+ 1` in `range` is important so the loop reaches the actual goal number!
