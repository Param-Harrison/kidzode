### One way to solve it:

```python
import random

colors = ["Crimson", "Azure", "Gold", "Ivory", "Emerald"]
pick = random.choice(colors)

print("🎨 PICKING THE PERFECT COLOR...")
print("The lucky color of the day is:", pick)
```
