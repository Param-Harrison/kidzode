# Hints

## 🟢 Hint 1
Classes: `class Fighter:` with `__init__` and `attack`.

## 🟡 Hint 2
Loop: `while p.health > 0 and e.health > 0:`

## 🟠 Hint 3
Turn: `p.attack(e)` then `if e.health <= 0: break`

## 🔴 Hint 4
Enemy Turn: `e.attack(p)` then `if p.health <= 0: break`
