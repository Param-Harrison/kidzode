# Add Numbers

Letters are good, but numbers make passwords stronger!

We can get numbers easily too.

```python
import string

numbers = string.digits
print(numbers)
```

You'll see:
```
0123456789
```

All the digits ready to use!

## Try This!
Print `letters` AND `numbers`!
