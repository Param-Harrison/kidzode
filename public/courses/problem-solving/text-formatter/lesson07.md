# Replace All

It replaces EVERY time the word appears.

```python
text = "banana banana banana"
new_text = text.replace("banana", "apple")

print(new_text)
```

You'll see:
```
apple apple apple
```

Be careful! It changes everything!

## Try This!
Replace all "a"s with "o"s in "banana"!
