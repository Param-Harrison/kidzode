# Mini-Challenge 2: Smart Directory 🧠

Combine everything!

**Your Goal:**
1. Create `File` (with `size`) and `Directory` classes.
2. Add `add()`, `ls()`, and `get_size()` to Directory.
3. Create a "Video" folder.
4. Add "movie.mp4" (content: "video data...").
5. Print the listing and the total size.

Go build your file system! 🛠️
