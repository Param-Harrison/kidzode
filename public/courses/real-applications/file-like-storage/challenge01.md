# Mini-Challenge 1: Notebook 📓

Create a simple note storage system.

**Your Goal:**
1. Define a `Note` class (like File) with `title` and `body`.
2. Define a `Notebook` class (like Directory) with a `notes` list.
3. Add a method `add_note(note)` to Notebook.
4. Create a Notebook "Ideas".
5. Add a Note "Invention" ("Toaster that flies").
6. Print the notebook's contents.

Go get organized!
