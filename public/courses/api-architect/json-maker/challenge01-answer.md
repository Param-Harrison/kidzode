### One way to solve it:

```python
profile = {
    "username": "CodeNinja",
    "followers": 1200,
    "verified": True
}

print("User", profile["username"], "has", profile["followers"], "followers.")
```
