# CHALLENGE 2: Config File ⚙️

Apps use "Config Files" to remember settings.
Let's store some advanced settings using **Nested Dictionaries**.

### Your Task:
1. Create a `config` dictionary.
2. Add a `"video"` key which stores another dictionary with `"resolution"` and `"fps"`.
3. Print the "fps" value by accessing it deeply `[][]`.

### Achievement:
You parsed a configuration tree! 🌳
