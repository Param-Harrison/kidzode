# CHALLENGE 3: Data Validator ✅

APIs must check if data is correct.
Let's validate a user object before "saving" it.

### Your Task:
1. Create a `user = {"age": 15}`.
2. Check `if "email"` is **NOT** in `user`.
3. If missing, print "Error: Email missing".
4. Check `if user["age"] < 13`.
5. If too young, print "Error: Too young".
6. Else, print "User valid".

### Achievement:
You built an API data guard! 🛡️
