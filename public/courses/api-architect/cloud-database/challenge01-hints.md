# Hints for Delete User

## 🟢 Hint 1
Create a new empty list.

## 🟡 Hint 2
Filter logic:
```python
if user["id"] != target_id:
    new_list.append(user)
```

## 🟠 Hint 3
This effectively "deletes" the one we skipped.
