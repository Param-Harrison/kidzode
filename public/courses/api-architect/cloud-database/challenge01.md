# CHALLENGE 1: Delete User ❌

Someone deleted their account.
Remove them by ID.

### Your Task:
1. Create `users = [{"id": 1}, {"id": 2}]`.
2. Target ID is 1.
3. Loop through users (iterate over a copy `users[:]` or simple index trick).
4. For simple way: Create `new_users` list.
5. If `id != target`, add to `new_users`.
6. Print `new_users`.

### Achievement:
You safely removed records! 🗑️
