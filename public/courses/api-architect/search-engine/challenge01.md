# CHALLENGE 1: Case Insensitive 🔡

"Apple" and "apple" should be the same.
Computers treat them differently!
Let's fix it with `.lower()`.

### Your Task:
1. Create `doc = "Python is PYTHON"`.
2. Lowercase it: `doc.lower()`.
3. Split it.
4. Print the unique words (Try converting list to `set()` or just print list).

### Achievement:
You normalized your dataset! ⚖️
