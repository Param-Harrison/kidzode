# CHALLENGE 2: Word Counter 🔢

How many times does each word appear?
This is crucial for relevance.

### Your Task:
1. Create `words = ["a", "b", "a", "c"]`.
2. Create empty `counts = {}`.
3. Loop through words.
4. If word in counts, `counts[word] += 1`.
5. Else, `counts[word] = 1`.

### Achievement:
You built a Frequency Counter! 📊
