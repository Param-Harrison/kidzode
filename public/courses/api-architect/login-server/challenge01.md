# CHALLENGE 1: Sign Up 📝

New users want to join!
We need to add them to our dictionary.

### Your Task:
1. Create an empty `users = {}`.
2. Create variables `new_user = "sam"` and `new_pass = "p@ss"`.
3. Add them to the dictionary: `users[new_user] = new_pass`.
4. Print the `users` dictionary.

### Achievement:
You implemented a registration system! 🆕
