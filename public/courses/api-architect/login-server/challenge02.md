# CHALLENGE 2: Admin Check 👮

Some users are more powerful than others.
Let's check if the logged-in user is an "admin".

### Your Task:
1. Create `role = "guest"`.
2. Check `if role == "admin"`.
3. If yes, print "Full Access".
4. `else`, print "Read Only".

### Achievement:
You implemented Role-Based Access Control (RBAC)! 🛡️
