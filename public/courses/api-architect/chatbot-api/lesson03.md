# Keyword Logic 🧠

Smart bots look for **keywords** inside the message.
We use `in` to check if a word exists.

```python
if "help" in text:
    print("Sending support...")
```

### Your Goal
1. Create `text = "I need some help please"`.
2. Check `if "help" in text`.
3. If yes, print "Help menu opening...".
4. Check `if "quit" in text`.
5. If yes, print "Goodbye".

### Achievement
🧠 **AI Brain**: You implemented keyword detection!
