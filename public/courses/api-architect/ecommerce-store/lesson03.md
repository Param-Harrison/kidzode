# Updating Stock (Update) 🔄

Prices change. Stock goes down.
We need to **Update** data.
We find the item and change its value.

### The Logic
```python
item = shop[0]
item["price"] = 5
```

### Your Goal
1. Create `data = [{"val": 10}]`.
2. Get the first item: `x = data[0]`.
3. Change its value: `x["val"] = 20`.
4. Print the data list.

### Achievement
🔄 **Manager**: You modified live data!
